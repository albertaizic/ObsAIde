/** Stable identifiers used across the plugin. Treat these as public API. */

/** Name of the assistant persona shown throughout the UI. */
export const ASSISTANT_NAME = 'Aide';

/** Workspace view type for the chat sidebar. Never rename after release. */
export const CHAT_VIEW_TYPE = 'obsaide-chat';

/** Lucide icon used for the ribbon action and the chat view. */
export const AIDE_ICON = 'sparkles';

/** File (inside the plugin folder) used to persist conversations. */
export const CONVERSATIONS_FILE = 'conversations.json';
